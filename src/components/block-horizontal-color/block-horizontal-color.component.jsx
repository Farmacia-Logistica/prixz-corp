import React from 'react';

const BlockHorizontalColor = ({ width, height, backgroundColor }) => (
	<div
		style={{
			width,
			height,
			backgroundColor
		}}
	></div>
);

export default BlockHorizontalColor;
